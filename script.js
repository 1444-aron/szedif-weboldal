function toggleMenu() {
            const menu = document.getElementById("mobileMenu");
            // Ha rajta van az 'open' osztály, leveszi (bezárja), ha nincs, rárakja (kinyitja)
            menu.classList.toggle("open");
        }